/**
 * @summary
 * Centralized middleware exports.
 * Provides single import point for all application middleware.
 *
 * @module middleware
 */

export { errorMiddleware } from './error';
export { notFoundMiddleware } from './notFound';
